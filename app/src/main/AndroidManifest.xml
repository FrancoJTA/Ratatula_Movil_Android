<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ratatula_Cliente"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".principal.PedidoInfoActivity"
            android:exported="false" />
        <activity
            android:name=".principal.LocalDetailActivity"
            android:exported="false" />
        <activity
            android:name=".principal.HolderActivity"
            android:exported="false" />
        <activity
            android:name=".Start.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Start.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".Start.AccesActivity"
            android:exported="false" />
        <activity
            android:name=".Start.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>